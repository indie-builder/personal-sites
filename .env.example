# 公开问答与本机资料解析统一使用 BigModel Anthropic 兼容端点。
# 固定地址 https://open.bigmodel.cn/api/anthropic，不回退到按量端点。
BIGMODEL_API_KEY=
BIGMODEL_MODEL=glm-5.3-flash
ASK_MODEL=glm-5.3-flash

# 问答匿名会话：至少 32 个随机字符。浏览器指纹与 IP 只用于生成 HMAC，存储层不保存原始值。
# Vercel 上会把 JSONL 会话持久保存到私有的 Supabase Storage，不需要新增客户端变量。
ASK_SESSION_SECRET=
# 可选：会话 JSONL 文件的无活动清理时间（小时），默认 24。
ASK_SESSION_RETENTION_HOURS=24

# Supabase：同步脚本用 service role；网站仅用 publishable key 读取公开策展表。
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# 每日动态 Cron 专用密钥由 Supabase Vault 生成并持有；Vercel 不保存明文。

# 可选：用于线上读取公开 GitHub 仓库树和文件，避免匿名 GitHub API 限流。
# 只在服务端配置，绝不能使用 NEXT_PUBLIC_ 前缀。
GITHUB_TOKEN=
