> 2026-09-09 更新：本文为上一轮设计交付记录，其中“构建”与项目灯箱已按用户要求下线；当前范围以 PRODUCT.md 和 DESIGN.md 为准。

# 全站设计交付 · Emil Design Engineering

日期：2026-09-08。目标：先定义全站设计规则，再逐页实现、逐功能验证，交付可运行的网站及验收证据。

技能：项目内 `.agents/skills/emil-design-eng/SKILL.md`，来源 https://github.com/emilkowalski/skills/tree/main/skills/emil-design-eng 。保留安装前 DESIGN.md 与 .impeccable/design.json 的用户调整。

## 设计范围

沿用 PRODUCT.md 的「运行中的工程档案」与 DESIGN.md 的身份轨、黑白灰和连续阅读流。重新设计的重点是阅读节奏、控件反馈、导航效率和完整状态；不捏造内容。

| 页面 / 功能 | 设计决定 | 验收 |
| --- | --- | --- |
| `/` 首页 | 身份为锚；头像、外链、介绍、技术场有清晰阅读顺序；手机独立完整首页 | 首访/回访、外链、主题、手机进入栏目 |
| `/ai-news` 每日动态 | 日期与分类分工；事件标题先于摘要；时间等宽数字 | 日期定位、分类、分页、失败恢复、详情入口 |
| `/curation` 每日关注 | 判断与原文摘录分层；统一登记栏、标题与段落节奏 | 列表阅读、分页、日期、详情返回位置 |
| `/design` 设计收藏 | 沿用判断流，视频为独立操作面；不强制播放 | 播放/暂停、失败原文出口、详情上下文 |
| `/douyin` 抖音收藏 | 同一登记簿语言；转写与个人解析区分 | 日期、分页、详情、来源返回 |
| `/open-source` 开源关注 | 主题索引即时切换，选中态稳定；判读为主 | 主题、计数、空态、详情 |
| `/works` 构建 | 真实样张与项目判断；样张缩放只在精细指针悬停 | 横滚、灯箱前后/关闭/键盘、项目详情 |
| `/ask` 问一问 | 清晰输入边界、建议行、引用；发送/范围/清空共享反馈尺度 | 输入法、建议、范围、发送、等待、停止/重试、清空、引用、滚动 |
| `/ai-news/[id]` | 题名、导读、真实来源构成短阅读闭环 | 返回、主题、原文、长标题 |
| `/curation/[id]` / `/design/[id]` | 证据与解析对页；窄屏按阅读顺序堆叠 | 媒体、长文、表格、代码、来源、相邻条目、返回 |
| `/open-source/[slug]` | 判读→中文文档/仓库浏览→GitHub；键盘焦点明确 | 文档切换、树展开、文件加载/失败、外部出口 |
| `/works/[slug]` | 项目定位→样张→项目内索引→记录证据 | 锚点、媒体、记录、外部出口 |
| 关于我打印稿 | 保留受控拟物；关闭路径与焦点返回明确 | 打开、打印终态、Esc/背板/关闭 |
| 全局状态 | loading、空、失败、404 保持阅读语法；不隐藏可用导航 | 深浅、减少动态效果、320/390/横屏/桌面 |
| RSS / sitemap / robots / API | 机器接口保持既有结构，无视觉包装 | 既有回归检查 |

## 代码审查基线

| Before | After | Why |
| --- | --- | --- |
| 主题图标串行 320ms 退场与入场 | 即时主题，短促图标反馈；键盘无位移 | 高频切换不累积等待 |
| 栏目退出动画之后才发起路由 | 高频同级栏目即时导航；身份收拢保留用途 | 阅读者的目的地优先 |
| 控件触达与按压反馈分散 | 共用 44px 触达、120ms 轻按压、可见焦点 | 桌面与触屏反馈一致 |
| 媒体 hover 未限制设备 | 只在 hover:hover + pointer:fine 下缩放 | 触屏不留下悬停缩放 |
| 仓库树 focus-visible 清除 outline | 明确轮廓，与悬停底色分开 | 键盘当前位置可辨认 |
| 排版规则分散 | 正文行高、数字稳定性、长题名换行收敛 | 所有内容类型共享阅读节奏 |

## 交付阶段

- [x] 安装并读取指定技能，建立目标与全站页面清单。
- [x] 完成真实页面基线观察，整理并落实 DESIGN.md 规范。
- [x] 实现共享壳、列表、详情、媒体、问答与状态设计。
- [x] ego lite 逐页面、逐功能验证，记录实际通过与限制。
- [x] typecheck、lint、test、build 与 Playwright 回归。
- [x] 交付变更说明和验收报告。

验收证据后续追加；清单尚未勾选的项目不能宣称已完成。

## 验收记录

### 已交付

规范先于实现落盘；共享排版、导航、主题、问答控件、仓库焦点、灯箱与 404 已按规范完成。本轮不是重写公开内容或重建信息架构；现有栏目与功能在统一设计规则下保留。技能安装于项目而非用户全局，源码参考提交 `d23d7f88a2e21c9e4b1418c7abe420f5c1052ba7`。

| Before | After | Why |
| --- | --- | --- |
| 技术场深色用整体 invert，边缘变为黑块 | 独立的深色点阵、词条、渐隐与暂停控件颜色 | 让技术场融入身份轨底色 |
| 404 使用框架默认页 | 复用身份轨与短文阅读骨架，提供首页及动态出口 | 缺失内容时仍能继续阅读 |
| 灯箱每个过渡 figure 各含 live region | 单一播报节点位于进出场容器之外 | 切图时只播报当前样张 |
| 简介、新闻和策展行距不一致 | 简介和摘要 1.65、列表题名 1.45 | 长中文段落更易读 |

### 浏览器验证

通过 ego lite 在本地 Next 应用完成，task space 2；1440/1512 桌面、390×844、320×740、844×390 横屏。15 个路由样本包括全部 8 个栏目入口、6 个详情样本和 404；320 与横屏的全部样本无文档横向溢出，390 的栏目及代表详情无溢出。检查了深色与减少动态效果。为浏览器视口模拟，未做实体 iPhone/Android 硬件验证。

- 首页与导航：身份轨、手机紧凑头、同级跳转、回访；自动测试覆盖手机桥接与终态清理。
- 每日动态：日期与分类工具栏，分类选项正常；列表与详情内容可读取。分页与流式首屏由已有回归覆盖。
- 策展/设计/抖音：各自真实内容与来源上下文、详情返回；设计视频控件存在、reduced-motion 下不自动播放，实际触发播放后 readyState=3，随后暂停。
- 开源：Skills 与工作流筛选后显示 3 项；中文/仓库切换及文件树成功。README 请求在本次真实网络下失败，页面显示「暂时无法读取 GitHub 原始仓库。」；未将原文成功加载记为通过。
- 构建：样张带、灯箱打开、方向键切图、Esc/按钮退出、单一状态播报；灯箱专项回归通过。
- Ask：手机选择「关于我」、建议填入并聚焦、真实发送、等待、收到回答和引用、追问；消息滚动/清空/错误/输入法/触达/可访问性由现有自动回归覆盖。
- 关于我：弹层出现、Esc 关闭及焦点返回；打印与 reduced-motion 终态由自动回归覆盖。
- 404：深浅主题、返回首页与动态入口；窄屏与横屏正常。
- CDP 检查未捕获 Runtime.exceptionThrown。开发服务曾卡死，重启本项目进程后恢复；使用 3000 本地服务验证。

### 自动检查

| 检查 | 结果 |
| --- | --- |
| pnpm typecheck | 通过 |
| pnpm lint | 通过 |
| pnpm test | 29 个 Vitest 文件 / 103 项 + Node 81 项通过 |
| pnpm build | 通过，策展与设计详情保持 ISR 分类 |
| Playwright | 全量第二轮 33/35，通过修复后的灯箱专项 1/1；合计 34 个场景已通过，剩余 1 项数据健康告警 |
| pnpm git:safety | 通过，0 violations |
| git diff --check | 通过 |

全量回归剩余失败为 `public discovery endpoints remain machine readable` 中的数据健康 HTTP 断言。原因：X 策展超过 72 小时未更新，健康端点按既有规则返回 503；数据库 quick_check、FTS 一致性、每日动态、开源和构建均健康。本轮没有同步或发布数据，也没有放宽健康断言。RSS、sitemap 等检查在该失败断言之前通过。

完整日志位于本机 `/tmp/personal-sites-emil-final-tests.log`、`/tmp/personal-sites-emil-e2e-final.log`、`/tmp/personal-sites-emil-lightbox.log`、`/tmp/personal-sites-emil-build-final.log`。未提交或推送；用户既有 `.impeccable/design.json` 调整保留。

### 截图

- [桌面深色动态](screenshots/desktop-dark.png)
- [手机深色问答与引用](screenshots/ask-mobile-dark.png)
- [手机深色 404](screenshots/not-found-mobile-dark.png)
